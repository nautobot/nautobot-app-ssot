Reduced the work an IP Fabric sync does per object. Devices and VLANs are read in one query each rather than one per Location, a synced Interface is written once rather than up to four times, creating or deleting an object records one change log entry rather than one per write, the safe delete tag is resolved once per model rather than once per object, and objects are deleted in batches rather than one at a time.

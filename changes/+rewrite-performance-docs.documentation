Rewrote the Performance chapter to cover the per-row cost shape of a sync, an expanded `bulk_create` recipe with audit-chain preservation via `web_request_context` and `deferred_change_logging_for_bulk_operation`, an IPAM-vs-DCIM safety matrix, validation strategies, memory considerations for large adapters, and Nautobot 2.x transaction behavior. Added Performance & Validation Design Notes and Reference under the Developer Guide capturing the design proposal in PR #1194 with explicit `(today)` / `(proposed)` markers throughout. Restructured the App Overview's feature list with the same accuracy markers.
